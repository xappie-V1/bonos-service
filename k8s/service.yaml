apiVersion: v1
kind: Service
metadata:
  name: bonos-service
  labels:
    app: bonos-service
spec:
  # ClusterIP: http://bonos-service:8004
  type: ClusterIP
  selector:
    app: bonos-service
  ports:
    - name: http
      protocol: TCP
      port: 8004
      targetPort: 8004
